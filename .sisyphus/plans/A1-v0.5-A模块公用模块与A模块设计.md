# A1-v0.5 A模块公用模块与A模块设计 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

> **版本**: v0.5 | **日期**: 2026-08-19 | **来源**: [[a-module-fullstack]]（2026-08-19修订版）
> **定位**: ⚠️ **本计划是A模块唯一实际执行计划**（公用底座 + 四工作台 + 断点A-H修复）。a-module-fullstack.md **不执行**，仅作参考架构文档与断点任务详细规格来源（其任务行号在第三篇中引用）。
>
> **批次划分（分批执行）**:
> - **批次1（本次执行）**: 第一篇公用模块 T1-T8 + 第二篇 T9 A1工作台 + 第三篇 T-DICT/T-B/T-A/T-C/T-F + T13a批次终验。A1依赖的断点链（词典→B→A→C→F）完整包含，无外部依赖。
> - **批次2（后续）**: T10 A2 + T-G/T-D/T-E/T-H（T-G依赖的T-F已在批次1就绪）
> - **批次3（后续）**: T11 A3 + T12 大厅 + T13b 全链路验收

**Goal:** 先建A模块公用底座（身份/编码/stale/Store/前端组件库），修复断点A-H使约束体系贯穿闭环，再在其上实现A1→A2→A3→GM大厅四工作台，跑通"选种子到大厅游玩"全链路。（分批执行：本次=公用+A1+断点{A,B,C,F}）

**Architecture:** 公用模块作为独立层先行交付（被四个工作台共同依赖）：身份层提供user_id+tier预留；GraphCodeIssuer实现四层编码原子发号；stale机制横切上下游版本链；图谱=事实源、文件=单向序列化导出。工作台层按7生成器"独立开发、独立可用、按需组合"原则构建，页面只是预编排串联。

**Tech Stack:** FastAPI + Pydantic v2 + aiosqlite（后端）；Vite + React 19 + TypeScript + Tailwind v4（前端，新页面"星空平行宇宙"主题）；pytest / Vitest / Playwright。

---

## 第一篇：A模块公用模块设计（先建底座）

> 依赖关系：T1→T3→T4；T2、T5、T6、T7 可并行；T8 依赖 T3。

### Task 1: 身份层（user_id + UserTier预留）

**Files:**
- Create: `backend/app/domains/identity/__init__.py`
- Create: `backend/app/domains/identity/key_manager.py`
- Create: `backend/app/domains/identity/tier.py`
- Create: `backend/app/api/identity_routes.py`
- Modify: `backend/app/main.py`（注册路由）
- Test: `backend/tests/unit/identity/test_key_manager.py`, `test_tier.py`

**Step 1: 写失败测试**

```python
# test_key_manager.py
def test_register_returns_unique_user_id_and_free_tier(store):
    a = key_manager.register(store)
    b = key_manager.register(store)
    assert a.user_id != b.user_id
    assert a.tier == "FREE"

# test_tier.py
def test_tier_enum_closed():
    assert {t.value for t in UserTier} == {"FREE", "VIP", "SVIP"}  # 不允许添加或修改
    assert UserTier.default() is UserTier.FREE
```

**Step 2:** `pytest tests/unit/identity/ -v` → FAIL（模块不存在）

**Step 3: 最小实现**

```python
# tier.py
class UserTier(str, Enum):
    FREE = "FREE"; VIP = "VIP"; SVIP = "SVIP"
    @classmethod
    def default(cls): return cls.FREE  # MVP无差异，仅预留

# key_manager.py
def register(store: UserStore) -> IdentityInfo:
    user_id = f"u_{uuid4().hex[:12]}"
    store.save(user_id, tier=UserTier.default().value)
    return IdentityInfo(user_id=user_id, tier="FREE")
```

路由：`POST /api/identity/register`，入参`{}`，出参`{user_id, tier:"FREE"}`。等级路由逻辑**不实现**（TODO注释）。

**Step 4:** pytest PASS。**Step 5:** `git commit -m "feat(identity): user_id注册+UserTier枚举预留"`

### Task 2: 前端API客户端基础设施

**Files:**
- Create: `frontend/src/api/client.ts`（fetchWithTimeout：统一超时/JSON解析/错误码抛出）
- Create: `frontend/src/api/identity.ts`（registerUser()）
- Test: `frontend/src/api/__tests__/client.test.ts`（Vitest，mock fetch断言超时与409Error.status）

关键签名：

```typescript
export async function fetchWithTimeout<T>(url: string, init?: RequestInit & {timeoutMs?: number}): Promise<T>
// 非2xx时抛 ApiError{status, body}；409 时 body 含 missing_sections
```

**Commit:** `feat(api-client): fetchWithTimeout+identity客户端`

### Task 3: 四层编码发号器 GraphCodeIssuer

**Files:**
- Create: `backend/app/domains/creation/shared/graph_code_issuer.py`
- Test: `backend/tests/unit/shared/test_graph_code_issuer.py`

**规则（逐字，不允许偏离）:**
- 格式 `IP{序号}-{阶段路径}-v{版本}`，例 `IP0142-M2-S1-v2`
- 阶段码封闭枚举：`W/M/S/TD/TC/R/G`（不允许添加或修改）
- IP序号=A1会话启动即分配，废弃归档不回收；实例号=同范围最大+1（S在其父M内递增，其余IP内递增）；版本号=同实例当前+1，**永不覆盖只追加**
- Store层原子递增（单写者事务），`(user_id, code)` 唯一约束
- 编码=检索显示层，主键=uuid；IP名只做display_name不进编码

**Step 1: 失败测试**

```python
def test_issue_and_bump(tmp_db):
    iss = GraphCodeIssuer(tmp_db)
    assert iss.new_ip("u1") == 1
    assert iss.next("u1", ip=1, stage="W") == "IP0001-W1-v1"
    assert iss.next("u1", ip=1, stage="W") == "IP0001-W1-v2"      # 版本追加
    assert iss.next("u1", ip=1, stage="M") == "IP0001-M1-v1"      # M在IP内递增
    assert iss.next("u1", ip=1, stage="S", parent_m=1) == "IP0001-M1-S1-v1"
    assert iss.next("u1", ip=1, stage="S", parent_m=1) == "IP0001-M1-S2-v1"

def test_concurrent_issue_is_atomic(tmp_db):  # 并发50线程发号，全部唯一
    ...
```

**Step 2:** FAIL → **Step 3:** 实现（SQL事务`SELECT max... INSERT`，唯一索引兜底重试）→ **Step 4:** PASS → **Step 5:** commit `feat(shared): 四层编码原子发号器`

### Task 4: 图谱清单API + stale标记机制

**Files:**
- Create: `backend/app/domains/creation/shared/stale_marker.py`
- Modify: `backend/app/api/graph_routes.py`（已存在222行，追加 `GET /api/graphs` 端点）
- Test: `backend/tests/unit/shared/test_stale_marker.py`

**stale规则（横切，逐字）:**
- 触发：上游重新定稿（如A1产出W1-v2）→ 自动标记所有 `parent_graph_code` 指向旧版本的下游图谱 stale
- **只标记不删除**，附变更集（diff摘要）
- 消费上游前检查：stale→提示变更集，**不阻断不删除**；用户坚持继续→留痕（`used_stale=true` 记录）

```python
def mark_downstream_stale(store, old_code: str, change_set: ChangeSet) -> int:
    """遍历 parent_graph_code=old_code 的下游，置 status='stale' 并挂 change_set。返回标记数。"""
```

API：`GET /api/graphs?user_id=` → 按IP分组清单 `{ips:[{ip_code, display_name, artifacts:[{graph_id, graph_code, stage, instance_no, version, status(finalized/stale), created_at, parent_graph_code}]}]}`，artifacts按created_at倒序。

**Commit:** `feat(shared): stale横切机制+用户图谱清单API`

### Task 5: Store扩展（图谱=事实源，文件单向导出）

**Files:**
- Create: `backend/app/state/structured_file_store.py`（SQLite：StructuredFile快照，编码与图谱版本同码，类型字段区分file/graph）
- Create: `backend/app/state/user_store.py`
- Modify: `backend/app/state/graph_store.py`（追加 `status`/`parent_graph_code`/`change_set` 字段+迁移）
- Test: `backend/tests/unit/state/test_structured_file_store.py`

核心断言：定稿后 `export_file_from_graph(graph) -> StructuredFile` 单向导出；反向写文件**不**回写图谱（重新定稿才重建）。

**Commit:** `feat(state): 文件快照Store+图谱血缘字段`

### Task 6: 语义编译契约（断点A接口，实现在第三篇T-A）

**Files:**
- Create: `backend/app/domains/creation/shared/semantic_compiler.py`（**仅Protocol契约+三级匹配骨架**）

```python
class SemanticCompiler(Protocol):
    def compile(self, session_id: str, text: str) -> CompileResult: ...
# CompileResult = 常规语句→{writes:[{field,value}]}（直接写入结构化文件）
#              | 创新语句→{classification_proposal:{suggestions:[{field,"其他"}]}}（二选一确认卡，用户确认前不落盘）
# 三级匹配：种子库级→词典规则级→LLM级（前两级零LLM成本）
```

测试用FakeCompiler（确定性）；真实词典+LLM实现在第三篇 T-B（断点A任务），完成后替换注入。**Commit:** `feat(shared): 语义编译三级匹配契约`

### Task 7: 前端公用组件库

**Files:**
- Create: `frontend/src/components/guided/GuidedChat.tsx`、`QuestionHint.tsx`
- Create: `frontend/src/components/structured/StructuredFilePanel.tsx`（实时diff渲染）、`DimensionProgress.tsx`（10板块完成度）、`DiffHighlight.tsx`
- Create: `frontend/src/components/graph/GraphSelector.tsx`（显式选上游：默认最新+stale徽章+变更集提示浮层；继续使用stale需二次确认"留痕使用/重新生成"）
- Create: `frontend/src/components/poster/PosterBoard.tsx`（展板骨架：全屏氛围图+渐变蒙版+设定浮层）
- Create: `frontend/src/components/IdentityGate.tsx`（LocalStorage无user_id→调register→显示ID+FREE徽章）
- Create: `frontend/src/components/TopNav.tsx`（A1→A2→A3→大厅 流程指示）
- Test: `frontend/src/components/__tests__/*.test.tsx`（Vitest+Testing Library：GraphSelector stale徽章渲染、DimensionProgress 10格、IdentityGate register调用）

**Commit:** `feat(ui): A模块公用组件库（星空主题token，不改旧CRT页面）`

### Task 8: 公用模块集成验收

**验收（curl+Playwright，证据存 `.sisyphus/evidence/v0.5-shared/`）:**
1. `POST /api/identity/register` → 200 `{user_id, tier:"FREE"}`
2. 连续`GET /api/graphs?user_id=`（造3版本数据）→ 编码按 `W1-v1/v2`、`M1/S1` 递增，倒序排列
3. 上游发v2 → 下游status=stale+change_set；`GET /api/graphs` 仍返回stale项（未删除）
4. GraphSelector组件：stale徽章可见、点击弹变更集、二选一确认卡出现

---

## 第二篇：A模块四工作台设计（基于第一篇底座）

> 生成器编号与 [[2-A-v0.3-A模块架构总览]] §4 保持一致：①IP设计 ②产品策划 ③主模组 ④骰子投影 ⑤角色投影 ⑥次级模组 ⑦资产生成。
> 依赖：A1依赖T1/T3/T5/T6/T7；A2依赖A1；A3依赖A2产物；大厅依赖A3。

### Task 9: A1工作台 /a1（生成器①：三态流）

**Files:**
- Create: `backend/app/domains/creation/seed/preset_loader.py`、`seed_generator.py`（权威源 `docs/governance/seed-presets-catalog.md` 8预设→运行时种子JSON，含10板块默认值预填）
- Create: `backend/app/domains/creation/seed/a1_question_tree.py`（10板块问题树+状态机，AI不发明问题）
- Create: `backend/app/domains/creation/a1/guide_engine.py`（状态机：用户不自由跳板块，LLM仅措辞/追问）
- Create: `backend/app/domains/creation/a1/innovation_capture.py`（创新语句→先匹配现有类别→AI提案→用户确认→落盘；"其他"仅存不一致内容）
- Create: `backend/app/domains/creation/a1/ip_poster.py`（氛围史诗图prompt+展板浮层数据）
- Create: `backend/app/api/a1_routes.py`
- Create: `frontend/src/pages/a1/A1Workspace.tsx`（单页三态：态①SeedSelector 8种子卡片/自定义输入 → 态②GuidedChat+StructuredFilePanel实时diff+DimensionProgress+定稿按钮（未齐不点亮）→ 态③内嵌IP展板；会话恢复按status路由）
- Create: `frontend/src/pages/a1/IPPoster.tsx`（`/a1/poster`：纯展板+【继续修改】→图谱条目浮层→【去修改】回工作台定位板块→重新定稿→W1-v2旧版保留）
- Test: `backend/tests/unit/a1/`（状态机顺序/三级匹配FakeCompiler/定稿校验/409）

**API契约（A1）：**

| 端点 | 方法 | 请求→响应 | 错误 |
|------|------|----------|------|
| `/api/a1/seeds` | GET | →`{seeds:[{id,name,genre,description,dimension_defaults}]}` | — |
| `/api/a1/session/start` | POST | `{user_id, seed_id?\|custom_idea?}`→`{session_id,file_id,ip_code,first_question,file}`（IP序号此时分配） | 400 二选一缺失 |
| `/api/a1/chat` | POST | `{session_id,message}`→`{reply,next_question,file_diff,progress,phase,classification_proposal?}` | — |
| `/api/a1/file/{id}` | GET | →StructuredFile（含status: draft/finalized, graph_id） | 404 |
| `/api/a1/file/{id}/finalize` | POST | →`{graph_id,graph_code,warnings}`（文件→图谱一次性转换：结构化条目→图谱节点+基础边；**约束建边由第三篇T-F（断点F）实现后注入此挂点**；触发下游stale标记） | **409**+`{missing_sections:[...]}` 10板块未齐 |
| `/api/a1/file/{id}/graph` | GET | →KnowledgeGraph（须已定稿） | 409 pending_finalize |
| `/api/a1/file/{id}/poster` | GET | →`{panels,ai_image_prompt,ai_image_status}` | **409** 未定稿 |

**TDD要点：** finalize缺板块→409+缺失清单；定稿后文件再改须重新定稿；重新定稿产出W1-v2且旧版保留、下游自动stale。

**Commit:** `feat(a1): 三态流工作台+定稿图谱化(409)+IP展板修改分支`

### Task 10: A2工作台 /a2（生成器②③④⑤：双路径汇合）【批次2】

**Files:**
- Create: `backend/app/domains/creation/a2/planning_analyzer.py`（策划分析+多轮对话决策循环）、`report_generator.py`（报告，免费完整实现）
- Create: `backend/app/domains/creation/a2/dice_projector.py`（④：输入=A1图谱，须含ACT约束/骰子设定；骰子模板图谱v+1版本化）
- Create: `backend/app/domains/creation/a2/character_projector.py`（⑤：输入=A1图谱+骰子模板，二者一起）
- Create: `backend/app/domains/creation/a2/module_finalizer.py`（③主模组生成=汇合点）、`module_poster.py`
- Create: `backend/app/api/a2_routes.py`
- Create: `frontend/src/pages/a2/A2Workspace.tsx`（GraphSelector选A1图谱（默认最新已定稿+stale徽章）→ **Step3路径选择⭐**：路径A卡片（完整策划线）/路径B卡片（跳过策划·TRPG默认线⚡）→ 路径A：GuidedChat决策对话+报告面板+决策落定；路径B：骰子投影→角色投影两步卡；汇合：生成主模组CTA；亦提供"跳过策划直接生成主模组"入口）
- Create: `frontend/src/pages/a2/A2Boards.tsx`（`/a2/boards`：骰子/角色/主模组展板tab切换+PromptGroupViewer提示词组）
- Test: `backend/tests/unit/a2/`（双路径汇合/投影版本化/免费线不带decisions可用）

**API契约（A2）：**

| 端点 | 方法 | 请求→响应 | 说明 |
|------|------|----------|------|
| `/api/a2/analyze` | POST | `{user_id, upstream_graph_id?}`→`{session_id,analysis,questions}` | 路径A启动；上游stale时响应附变更集提示 |
| `/api/a2/decide` | POST | `{session_id,decisions}`→`{a2_prime_file,free_path:"TRPG"}` | 决策落定（A2'文件） |
| `/api/a2/report` | GET | `?session_id=`→PlanningReport | 免费完整实现 |
| `/api/a2/file/{id}/dice-template` | POST | `{upstream_graph_id?}`→DiceTemplate | ④骰子投影（上游=A1图谱）※端点名为当前实现约定，流程图未指定 |
| `/api/a2/file/{id}/character-template` | POST | `{upstream_graph_id?,dice_template_id?}`→CharacterTemplate | ⑤角色投影（图谱+骰子模板）※同上 |
| `/api/a2/file/{id}/finalize` | POST | `{upstream_graph_id?,decision_session_id?}`→`{module_graph_code,module_poster,prompt_group,a2_double_prime_file}` | **③汇合点**：不带策划=免费TRPG默认线；带=策划增强线；④⑤后亦汇合于此。产出IPxxxx-M1-v1，TRPG默认标注+下位拓展 |

**TDD要点：** finalize不带decision_session_id也成功（免费线）；骰子→角色依赖校验（缺骰子模板→409）；模板v+1不覆盖。

**Commit:** `feat(a2): 双路径架构（策划线∥TRPG默认线）汇合主模组生成`

### Task 11: A3工作台 /a3（生成器⑥⑦）【批次3】

**Files:**
- Create: `backend/app/domains/creation/a3/scene_expander.py`（⑥：主模组图谱+scope+counts→次级模组图谱IPxxxx-M1-S1-v1，场景清单+资产清单）
- Create: `backend/app/domains/creation/a3/asset_prompt_extractor.py`（标签→约束注入提示词组）
- Modify: 既有 `GenerationScheduler`（复用Wave分组串行调度+异步job）
- Create: `backend/app/api/a3_routes.py`
- Create: `frontend/src/pages/a3/A3Workspace.tsx`（GraphSelector选主模组图谱（默认最新+stale标记）→扩写对话（scope/counts参数）→场景/资产清单→提示词组查看→图片生成进度（Wave分组轮询）；图片版本化、审批后才可用）
- Test: `backend/tests/unit/a3/`

**API契约（A3）：**

| 端点 | 方法 | 请求→响应 | 说明 |
|------|------|----------|------|
| `/api/a3/expand` | POST | `{user_id, upstream_graph_id?, scope, counts}`→`{secondary_graph_code,file,graph}` | ⑥显式选上游（默认最新主模组图谱） |
| `/api/a3/file/{id}/prompts` | GET | →`[{asset_id,prompt}]` | 提示词组 |
| `/api/a3/assets/generate` | POST | `{upstream_graph_id?,scope,counts}`→`{job_id,status}` | ⑦异步任务+Wave分组轮询进度 |

**Commit:** `feat(a3): 场景资产扩写+异步图片生成(Wave轮询+版本化审批)`

### Task 12: GM大厅 /lobby（装配消费）【批次3】

**Files:**
- Create: `backend/app/domains/creation/a3/lobby_loader.py`（装配数据包：已审批资产+角色卡+骰子面板+场景图）
- Create: `backend/app/api/lobby_routes.py`
- Create: `frontend/src/pages/lobby/Lobby.tsx`（SceneView增强：载入A3场景图 + CharacterCardPanel + DicePanel + Terminal复用）
- Test: `backend/tests/unit/a3/test_lobby_loader.py`（仅approved资产入包）

**API契约：** `GET /api/lobby?graph_code=?`（默认最新可装配图谱）→`{scenes,images,character_card,dice_panel}`

**Commit:** `feat(lobby): GM大厅装配（场景图+角色卡+骰子面板+终端）`

### Task 13: 全链路验收（curl+Playwright）

1. `register→seeds→session→chat×10→finalize(A1图谱化)→graph→poster→analyze→decide→dice/character→finalize(A2)→expand→generate→lobby` 全部200
2. 未定稿访问 graph/poster/analyze → 409/pending；finalize缺板块 → 409+缺失清单
3. A1重新定稿W1-v2 → 下游M1-v1显示stale徽章，不阻断可继续（留痕）
4. Playwright四页冒烟：/a1 /a2 /a3 /lobby 可交互，星空主题生效且旧CRT页面样式未被改动
5. 证据存 `.sisyphus/evidence/v0.5-final/`

---

## 第三篇：断点A-H修复（本计划实际执行部分）

> **权威规格来源**: `.sisyphus/plans/a-module-fullstack.md` 对应任务节（行号见下），执行时**先读源节再动手**；断点定义权威= `.sisyphus/drafts/A模块层级图-含断点.md:105-375`。
> **依赖链（不可乱序）**: 词典→断点B→断点A→断点C→断点F；F→G；G→E；{D,E}并行；H独立。
> **统一协议（每任务）**: ①授权关卡——第一优先步骤：向用户呈现解释（现状/修复/影响文件/风险与回滚，引用T2授权文档=源计划L398-403），**停下等用户明确授权**，授权话语存 `.sisyphus/evidence/task-{X}-authorization.txt` ②pytest基线留存 ③TDD实现 ④回归验证（基线测试全绿）⑤证据留痕。

### Task T-DICT: 标签+枚举词典（断点A数据层）
**源规格**: a-module-fullstack.md L488-519。新建 `backend/app/config/tag_dictionary.yaml`：全部合法标签与枚举逐字来自层级图L105-130（LAW: world_structure/gravity/conservation/divine_intervention/afterlife；ACT: dice_mode/check_direction/cost_function/core_action；NAR: era_stage/time_mode/trajectory/success_granularity；WST: cost_type/feedback_loop/climate_zone/power_saturation；SOC: political_type/access_topology/threshold/economy_type）+ A2新增 LOC（loc_topology/loc_connectivity）与 STY（style_keywords/architecture_style）标签组。枚举值**逐字照抄，不允许添加或修改**。TDD：YAML加载校验（6维齐全+LOC/STY）。

### Task T-B: ⚠️断点B修复：Output模型结构化字段
**源规格**: L789-824。授权关卡→修改 `backend/app/models/dimension.py` 6个Output：按断点B清单加结构化字段（字段表见L795，**全部Optional+默认None向后兼容**，自由文本字段保留）→调用方适配+旧测试迁移。

### Task T-A: ⚠️断点A修复：语义→标签→枚举编译器
**源规格**: L857-884。授权关卡→实现 `backend/app/domains/creation/a1/semantic_compiler.py` 三级匹配（种子库级→词典规则级→LLM级，前两级零LLM成本），输出`标签=枚举值`写入结构化文件；完成后替换第一篇T6的FakeCompiler注入。LLM单测用FakeProvider。

### Task T-C: ⚠️断点C修复：约束应用逻辑树
**源规格**: L913-918。授权关卡→新建 `backend/app/domains/creation/constraint/application_tree.py`（字段照层级图断点C节逐字：source_field/target_prompt_layer/target_node_field/target_edge_type/transform/priority）+ `application_tree.yaml`（6维×6层=36条映射）。

### Task T-F: ⚠️断点F修复：约束→拓扑节点/边创建
**源规格**: L965-970。授权关卡→新建 `backend/app/domains/creation/graph/constraint_topology.py`；完成后**注入A1 finalize挂点**（第二篇Task 9预留），使定稿图谱化含约束建边。依赖：T-B、T-C。

### Task T-G: ⚠️断点G修复：约束继承传递【批次2】
**源规格**: L1310-1315。授权关卡→新建 `backend/app/domains/creation/constraint/constraint_inheritor.py`：沿图谱边传递约束，按权重过滤密度（≥10%完整传递/5-10%压缩摘要/<5%不传），override可但不可删除。依赖：T-F。

### Task T-D: ⚠️断点D修复：PromptBuilder约束注入【批次2】
**源规格**: L1670-1673。授权关卡→修改 `backend/app/domains/creation/asset/prompt_builder.py`：8层模板引用约束标签（按T-C逻辑树target_prompt_layer路由）。

### Task T-E: ⚠️断点E修复：PromptFusion约束感知【批次2】
**源规格**: L1723。授权关卡→修改 `backend/app/domains/creation/asset/prompt_fusion.py`：3段式组装感知约束（依赖T-G的继承结果）。依赖：T-G。

### Task T-H: ⚠️断点H修复：层间语义焦点模板【批次2，独立】
**源规格**: L1769。授权关卡→修改 `DimensionPromptBuilder`（dimension_generator.py）：同一维度在6层的不同语义焦点提示模板。独立，可与D/E并行。

---

## 执行顺序与并行（分批：本次执行批次1，断点修复与工作台交织）

### 批次1（本次）：公用 + A1 + 断点{词典,B,A,C,F}
```
Wave 1（公用底座）: T1→T3→T4 串行；T2/T5/T6/T7 并行；T-DICT 同波并行
Wave 2: T-B（断点B）→ T-A（断点A，替换T6 FakeCompiler）；T8（公用验收）并行
Wave 3: T-C（断点C）→ T-F（断点F）→ T9（A1，finalize挂点接入约束建边）
Wave FINAL-1: T13a 批次1终验（A1链路）:
  1. register→seeds→session→chat×10→finalize(A1图谱化)→graph→poster 全部200
  2. 未定稿访问 graph/poster→409/pending；finalize缺板块→409+缺失清单
  3. 定稿图谱断言：约束建边存在（T-F生效，节点/边含constraint类型）
  4. 重新定稿W1-v2→旧版保留、下游自动stale、不阻断
  5. Playwright：/a1三态流+IP展板可交互，星空主题生效，旧CRT页面未改
```

### 批次2（后续）：A2 + 断点{G,D,E,H}
```
T-G（依赖批次1的T-F）∥ T-H（独立）∥ T10（A2双路径）→ Wave后A2链路验收
```

### 批次3（后续）：A3 + 大厅 + 全链路
```
T11（A3，提示词组接入T-D/E注入）→ T12（大厅）→ T13b 全链路终验
```

**断点授权关卡提醒**: T-B/T-A/T-C/T-F（批次1）+ T-G/T-D/T-E/T-H（批次2）每个任务开工前**必须停下等用户授权**（协议见第三篇头部），未获授权不得写代码。

## Success Criteria

- [x] pytest / vitest / npm run build 全绿（含断点任务pytest基线回归）
- [x] curl 全链路（含409错误场景）通过，证据齐全（含8个断点任务的authorization.txt）
- [x] 断点A-H全部修复：约束体系贯穿"采集→编译→生成→注入→继承→产出"闭环
- [x] 7生成器独立可用（只要IP/只要主模组/只要次级模组均可单独交付）
- [x] 四层编码、修改不污染、stale不阻断三条铁律有测试覆盖

